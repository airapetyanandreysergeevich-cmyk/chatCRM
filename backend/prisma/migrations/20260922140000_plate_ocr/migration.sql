-- Распознавание шильдиков камерой: включается и выключается собственником
-- платформы для каждой мастерской отдельно. По умолчанию включено.
--
-- Tenant — таблица платформы, построчной защиты на ней нет, поэтому режим
-- платформы здесь не нужен.
ALTER TABLE "Tenant" ADD COLUMN "plateOcr" BOOLEAN NOT NULL DEFAULT true;
